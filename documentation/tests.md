# Verification

Pull requests run Vitest, TypeScript checking, and the production Next.js build. Contract tests cover allowed sources and types, trusted links, idempotency, duplicate delivery, Forms metadata redaction, lifecycle deletion, and legacy-route behavior.
