# Runtime variables

Core runtime settings include Supabase server credentials, User System integration, push configuration, and `EVENT_INGRESS_SECRET`. Lifecycle deletion uses a separate credential. Public links supplied by producers must target trusted ChemVault HTTPS origins.
